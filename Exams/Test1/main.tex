\documentclass[]{exam}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{multicol}
\usepackage[a4paper,width=150mm,top=25mm,bottom=25mm]{geometry}
\usepackage{graphicx}
% \title{Innopolis University \\ Analytical Geometry and Linear Algebra~I
% \\ Test I. }
% (DRAFT COPIED FROM 2020) }
\date{}

\begin{document}
 
%\maketitle

\addpoints
%\gradetable[h][questions]
\
\centering
AGLA 1. TEST 1. \textbf{VARIANT 1.} 15 points, 60 minutes
\bigskip

\begin{tabular}{cc}
    \begin{tabular}[b]{|p{11cm}|c|}
    \hline
    Full name: & Group: \\ \hline
     &  \\ 
     & \\
     \hline
    \end{tabular}
\\

    \begin{tabular}[b]{|c|c|c|c|c|c|c|c|}
    \hline
    Task: & 1 & 2 & 3 & 4 & 5 & 6 & Total \\ \hline

    Score: & &  & &  &  & & \\ \hline
    \end{tabular}
\end{tabular}


\begin{enumerate}
\large 
\item (2 points) For each of the following statements mark it as True or False. Justify each answer.
\smallskip
% \vskip


\begin{enumerate}

\begin{tabular}[b]{p{10cm}|p{3cm}}

\hline
   \item  If matrix $B$ is produced by interchanging two columns of matrix $A$, then $\det (B) = -\det (A) $. 
   
   Explain your answer in $2\times 2$ case. &    \\ 
%   \hline
%     \item The result of Scalar triple product operation is a vector. & \\
    \hline
    \item For any square matrix $A$ there exists exactly one inverse matrix. & \\
    \hline
    \end{tabular}
\end{enumerate}

\item (2 points) Decompose the vector $\textbf{p} = (1, 2, 3)$ into components parallel and perpendicular to the vector $\textbf{q} = (1, -2, 2)$.

\item (2 points)
\begin{enumerate}
    \item Find the matrix product $AB$ if $A=\begin{bmatrix}x & -2 & -1 \\ 4 & 1 & -4 \end{bmatrix}$, $B=\begin{bmatrix} -5 & 1 \\ 1 & -3 \\ 2 & x \end{bmatrix}$
    \item Find the largest possible value of determinant $(AB)$.
\end{enumerate}

\item (3 points)  Point $A$ has coordinates (5; -1; 8) in the old coordinate system. Find its coordinates in the new coordinate system obtained from the initial one by transferring the origin to point $N$ that has coordinates (33; -1; 2) in the old coordinate system.

\item (3 points) Subspace $S$ of $\mathbb{R}^3$ is formed by linear combination of vectors $v_1$ and $v_2$. Find a vector $v$ that is orthogonal to $S$, if $v_1 = \begin{bmatrix}1 \\ 2\\ 3\end{bmatrix}$, $v_2 = \begin{bmatrix}4 \\ 5\\ 6\end{bmatrix}$

\item (3 points) Let \begin{equation}
    \mathbf{A}=\begin{bmatrix}
       0  & 1 & 0 \\
       0  & 0 & 1 \\
       1  & 0 & 0 
    \end{bmatrix}
    \end{equation}
Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $A^k=A^{-1}$ ( Also you need to check if $A$ is invertible), Note that $A^k=\underbrace{A.A \dots A}_{k \ times} $

% \item The volume of the tetrahedron $V=5$. Three of its vertices are $A(2;\,1;\,-1)$, $B(3;\,0;\,1)$ and $C(2;\,-1;\,3)$.
% Find all possible coordinates of the vertex $D$ given that it lies on y-axis.

% \item $ABCDEF$ is a regular hexagon. Find coordinates $(x,\,y)$ of point $M$ in coordinate system $A$, $\overrightarrow{AB}$, $\overrightarrow{AF}$ given its coordinates $(x',\,y')$ in coordinate system $D$, $\overrightarrow{CB}$, $\overrightarrow{CE}$.

% \item Let $\mathcal{V}=\{(x,y,z)\in \mathbb{R}^3 : x+y+z=0\}$ . Prove that $\mathcal{V}$ is a vector subspace in $\mathbb{R}^3$.

% \item Prove that a matrix $\mathbf{A}\in \mathcal{M}_2(\mathbb{R})$ (2x2 Matrix and its elements are real numbers) doesn't exist, where:
% \begin{equation}
%     \mathbf{A}^2=\begin{bmatrix}
%       0  & 1 \\
%       0  & 0
%     \end{bmatrix}
% \end{equation}





% \begin{align*}
%     w_1=\frac{\vec{p}\cdot\vec{q}}{|\vec{p}|}=\frac{3}{\sqrt{14}}\\
%     w_2=\frac{\vec{p}\cdot\vec{q}}{|\vec{q}|}=\frac{3}{3}=1
% \end{align*}


\end{enumerate}

\begin{center}
    End of Test 1
\end{center}

% =================================% =================================
% =================================% =================================

% VARIANT 2
% =================================% =================================
%Amer
\newpage 

AGLA 1. TEST 1. \textbf{VARIANT 2.} 15 points, 60 minutes
\bigskip

\begin{tabular}{cc}
    \begin{tabular}[b]{|p{11cm}|c|}
    \hline
    Full name: & Group: \\ \hline
     &  \\ 
     & \\
     \hline
    \end{tabular}
\\

    \begin{tabular}[b]{|c|c|c|c|c|c|c|c|c|c|c|}
    \hline
    Task: & 1 & 2 & 3 & 4 & 5 & 6 & 7 & 8 & 9 & Total \\ \hline

    Score: & &  &  &  &  &  & &  &  & \\ \hline
    \end{tabular}
\end{tabular}

\begin{enumerate}
\large 
\item (2 points) For each of the following statements mark it as True or False. Justify each answer.
\smallskip


\begin{enumerate}

\begin{tabular}[b]{ p{10cm} | p{3cm} }

\hline
   \item  For any matrices $A, B, C$: $(ABC)^{\top} = C^{\top}B^{\top}A^{\top}$
    &    \\ 
%   \hline
%     \item The result of Scalar triple product operation is a vector. & \\
    \hline
    \item Inverse matrix ($A^{-1}$) always exists. & \\
    \hline
    \end{tabular}
\end{enumerate}

\item (2 points) Decompose the vector $\textbf{p} = (2, 4, 6)$ into components parallel and perpendicular to the vector $\textbf{q} = (1, 2, -2)$.

\item (2 points)
\begin{enumerate}
    \item Find the matrix product $AB$ if $A=\begin{bmatrix}4 & -2 & 1 \\ 2 & x & -5 \end{bmatrix}$, $B=\begin{bmatrix} 3 & -1 \\ 2 & 2 \\ x & -3 \end{bmatrix}$
    \item Find the largest possible value of determinant $(AB)$.
\end{enumerate}

\item (3 points)  Point $A$ has coordinates (-3; 29; -1) in the old coordinate system. Find its coordinates in the new coordinate system obtained from the initial one by transferring the origin to point $N$ that has coordinates (1; -1; 2) in the old coordinate system.

\item (3 points) Subspace $S$ of $\mathbb{R}^3$ is formed by linear combination of vectors $v_1$ and $v_2$. Find a vector $v$ that is orthogonal to $S$, if $v_1 = \begin{bmatrix}2 \\ 2\\ 1\end{bmatrix}$, $v_2 = \begin{bmatrix}5 \\ 4\\ 2\end{bmatrix}$

\item (3 points) Let \begin{equation}
    \mathbf{A}=\begin{bmatrix}
       0  & 0 & 1 \\
       1/5  & 0 & 0 \\
       0  & 5 & 0 
    \end{bmatrix}
    \end{equation}
Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $A^k=A^{-1}$ ( Also you need to check if $A$ is invertible), Note that $A^k=\underbrace{A.A \dots A}_{k \ times} $

% \item Let $\mathcal{V}=\{(x,2x,3x)\in \mathbb{R}^3 ; x\in\mathbb{R}\}$ . Prove that $\mathcal{V}$ is a vector subspace in $\mathbb{R}^3$.

% \item Let \begin{equation}
%     \mathbf{A}=\begin{bmatrix}
%       0  & 1 & 0 \\
%       0  & 0 & 1 \\
%       1  & 0 & 0 
%     \end{bmatrix}
%     \end{equation}
% Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $\mathbf{A}^k=\mathbf{A}^{-1}$( Also you need to check if $\mathbf{A}$ is invertible)

% \item Find all matrices $\mathbf{A} \in \mathcal{M}_2(\mathbb{R})$ (2x2 Matrix and its elements are real numbers) that satisfies the following:

% \begin{equation}
%     \mathbf{A}^2=\begin{bmatrix}
%       0  & 0 \\
%       0  & 0
%     \end{bmatrix}
% \end{equation}
% ...
\end{enumerate}

\begin{center}
    End of Test 1
\end{center}


% =================================% =================================
% =================================% =================================

% VARIANT 3
% =================================% =================================
\newpage 

AGLA 1. TEST 1. \textbf{VARIANT 3.} 15 points, 60 minutes
\bigskip

\begin{tabular}{cc}
    \begin{tabular}[b]{|p{11cm}|c|}
    \hline
    Full name: & Group: \\ \hline
     &  \\ 
     & \\
     \hline
    \end{tabular}
\\

    \begin{tabular}[b]{|c|c|c|c|c|c|c|c|c|c|}
    \hline
    Task: & 1 & 2 & 3 & 4 & 5 & 6 & 7 & 8 & Total \\ \hline

    Score: &  &  &  &  &  & &  &  & \\ \hline
    \end{tabular}
\end{tabular}

\begin{enumerate}
\large 
\item (2 points) For each of the following statements mark it as True or False. Justify each answer.
\smallskip


\begin{enumerate}

\begin{tabular}[b]{ p{10cm} | p{3cm} }

\hline
   \item   By definition, a matrix $C$ is orthogonal iff $C^{-1}=C$ . &    \\ 
%   \hline
%     \item The result of Scalar triple product operation is a vector. & \\
    \hline
    \item The determinant of a matrix always defined. & \\
    \hline
    \end{tabular}
\end{enumerate}

\item (2 points) Decompose the vector $\textbf{p} = (3, 6, 9)$ into components parallel and perpendicular to the vector $\textbf{q} = (1, -2, 2)$.

\item (2 points)
\begin{enumerate}
    \item Find the matrix product $AB$ if $A=\begin{bmatrix}2 & x & 5 \\ 4 & 3 & -2 \end{bmatrix}$, $B=\begin{bmatrix} 1 & x \\ -3 & 2 \\ -1 & 2 \end{bmatrix}$
    \item Find the largest possible value of determinant $(AB)$.
\end{enumerate}

\item (3 points)  Point $A$ has coordinates (2; -4; 6) in the old coordinate system. Find its coordinates in the new coordinate system obtained from the initial one by transferring the origin to point $N$ that has coordinates (7; 1; -4) in the old coordinate system.

\item (3 points) Subspace $S$ of $\mathbb{R}^3$ is formed by linear combination of vectors $v_1$ and $v_2$. Find a vector $v$ that is orthogonal to $S$, if $v_1 = \begin{bmatrix}3 \\ 3\\ 5\end{bmatrix}$, $v_2 = \begin{bmatrix}4 \\ 4\\ 2\end{bmatrix}$

\item (3 points) Let \begin{equation}
    \mathbf{A}=\begin{bmatrix}
       -1  & 0 & 0 \\
       0  & 0 & -1 \\
       0  & 1 & 0 
    \end{bmatrix}
    \end{equation}
Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $A^k=A^{-1}$ ( Also you need to check if $A$ is invertible), Note that $A^k=\underbrace{A.A \dots A}_{k \ times} $

% \item Let \begin{equation}
%     \mathbf{A}=\begin{bmatrix}
%       0  & 1 & 0 \\
%       0  & 0 & 1 \\
%       1  & 0 & 0 
%     \end{bmatrix}
%     \end{equation}
% Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $\mathbf{A}^k=\mathbf{A}^{-1}$( Also you need to check if $\mathbf{A}$ is invertible)
% ...
\end{enumerate}

\begin{center}
    End of Test 1
\end{center}



% =================================% =================================
% =================================% =================================

% % VARIANT 4
% % =================================% =================================
\newpage 

AGLA 1. TEST 1. \textbf{VARIANT 4.} 15 points, 60 minutes
\bigskip

\begin{tabular}{cc}
    \begin{tabular}[b]{|p{11cm}|c|}
    \hline
    Full name: & Group: \\ \hline
     &  \\ 
     & \\
     \hline
    \end{tabular}
\\

    \begin{tabular}[b]{|c|c|c|c|c|c|c|c|c|c|}
    \hline
    Task: & 1 & 2 & 3 & 4 & 5 & 6 & 7 & 8 & Total \\ \hline

    Score: &  &  &  &  &  & &  &  & \\ \hline
    \end{tabular}
\end{tabular}

\begin{enumerate}
\large 
\item (2 points) For each of the following statements mark it as True or False. Justify each answer.
\smallskip


\begin{enumerate}

\begin{tabular}[b]{ p{10cm} | p{3cm} }

\hline
   \item The determinant of a transition matrix (between two bases) can be any real number. &    \\ 
%   \hline
%     \item The result of Scalar triple product operation is a vector. & \\
    \hline
    \item Any straight line is a subspace of $R^2$ & \\
    \hline
    \end{tabular}
\end{enumerate}

\item (2 points) Decompose the vector $\textbf{p} = (2, 4, 6)$ into components parallel and perpendicular to the vector $\textbf{q} = (1/3, -2/3, 2/3)$.

\item (2 points)
\begin{enumerate}
    \item Find the matrix product $AB$ if $A=\begin{bmatrix}1 & 2 & 5 \\ 3 & 7 & x \end{bmatrix}$, $B=\begin{bmatrix} 5 & -1 \\ x & 2 \\ -3 & -1 \end{bmatrix}$
    \item Find the largest possible value of determinant $(AB)$.
\end{enumerate}

\item (3 points)  Point $A$ has coordinates (1; -1; -19) in the old coordinate system. Find its coordinates in the new coordinate system obtained from the initial one by transferring the origin to point $N$ that has coordinates (4; -5; 2) in the old coordinate system.

\item (3 points) Subspace $S$ of $\mathbb{R}^3$ is formed by linear combination of vectors $v_1$ and $v_2$. Find a vector $v$ that is orthogonal to $S$, if $v_1 = \begin{bmatrix}1 \\ 3\\ 2\end{bmatrix}$, $v_2 = \begin{bmatrix}3 \\ 1\\ 2\end{bmatrix}$

\item (3 points) Let \begin{equation}
    \mathbf{A}=\begin{bmatrix}
       -1  & 0 & 0 \\
       0  & 0 & -1/7 \\
       0  & -7 & 0 
    \end{bmatrix}
    \end{equation}
Find All \textbf{Natural} numbers ($k\in \mathbb{N}$) where: $A^k=A^{-1}$ ( Also you need to check if $A$ is invertible), Note that $A^k=\underbrace{A.A \dots A}_{k \ times} $
...
\end{enumerate}

\begin{center}
    End of Test 1
\end{center}


\newpage 


% %% DEMO 

% VARIANT DEMO (to share with students). 15 points, 60 minutes
% \bigskip


% \begin{tabular}{cc}
%     \begin{tabular}[b]{|p{11cm}|c|}
%     \hline
%     Full name: & Group: \\ \hline
%      &  \\ 
%      & \\
%      \hline
%     \end{tabular}
% \\

%     \begin{tabular}[b]{|c|c|c|c|c|c|c|c|}
%     \hline
%     Task: & 1 & 2 & 3 & 4 & 5 & 6 & Total \\ \hline

%     Score: & &  & &  &  & & \\ \hline
%     \end{tabular}
% \end{tabular}


% \begin{enumerate}
% \large 
% \item (2 points) For each of the following statements mark it as True or False. Justify each answer.
% \vskip


% \begin{enumerate}

% \begin{tabular}[b]{ p{10cm} | p{3cm} }

% \hline
%   \item  For example, two 3-dimensional vectors always span 2D plane, True or False?  &    \\ 
% %   \hline
% %     \item The result of Scalar triple product operation is a vector. & \\
%     \hline
%     \item ... & \\
%     \hline
%     \end{tabular}
% \end{enumerate}

% \item (2 points) Decompose the vector $\textbf{p} = ... $ into components parallel and perpendicular to the vector $\textbf{q} = ...$.

% \item (2 points)
% \begin{enumerate}
%     \item Find the matrix product $AB$ if $A=... $, $B=...$
%     \item Find the largest possible value of determinant of their product $(AB)$.
% \end{enumerate}

% \item (3 points)  Point $A$ has coordinates (...) in the old coordinate system. Find its coordinates in the new coordinate system obtained from the initial one by transferring the origin to point $N$ that has coordinates (...) in the old coordinate system.

% \item (3 points) Subspace $S$ of $\mathbb{R}^3$ is formed by linear combination of vectors $v_1$ and $v_2$. Find a vector $v$ that is orthogonal to $S$, if $v_1 = ...$, $v_2 = ...$

% \item (3 points) Here is one more secret task.
% \end{enumerate}

% \begin{center}
%     End of Test 1
% \end{center}

\end{document}